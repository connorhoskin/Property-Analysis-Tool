// src/NewProperty.js
import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';

function NewProperty() {  // Match the component name with the export

    return (
        <div>
            <h1>Add a New Property</h1>
            <h1>hello my name is connor and this is my first time using react</h1>
        </div>  // Properly close the <div> and <h1> tags
    );
}

export default NewProperty;
